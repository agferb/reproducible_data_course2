# Commands

## Basic routine

Add to staging area
`git add <file_name>`

Add to local repository
`git commit -m "meaningful message"`

Push changes to remote repo
`git push`

# Creating branches
```
git branch <name>
git checkout <name>
```

or
`git checkout -b <name>`

To delete a branch:
`git branch -d <name>`